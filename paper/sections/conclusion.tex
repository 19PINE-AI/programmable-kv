\section{Conclusion}
\label{sec:conclusion}

A surgical edit to a field's KV is ignored not because the cache is fragile but because the model already
did the work: at prefill it memoizes the field-conditioned \emph{conclusion} onto downstream aggregator
tokens, and the decision reads those notes. Reframing the KV cache as a notebook of memoized conclusions
makes two capabilities fall out of one mechanism---you can \emph{edit} the notes (amend them with a late,
salient erratum rather than recompute) and \emph{compose} the notes (reposition and splice a precompiled
skill, in $O(L)$ time)---and a keystone experiment shows they act on the same notes. The mechanism holds for
any per-token attention KV cache: it spans scale, quantization, Mixture-of-Experts, and multimodal
image caches, transfers freely across throughput optimizations, and reaches Multi-head Latent Attention,
sliding-window, and interleaved-M-RoPE models through small adapters---up to the edge of the 2026
sparse/compressed-attention frontier, where the unit of editing and composition becomes a block rather
than a token. In practice this turns two everyday agent costs---reloading a library of skills and
re-reading a mutating user-memory document---into $O(L)$ cache operations that preserve the decision, with
up to $14.5\times$ online serving throughput. We hope the ``models take notes at prefill'' lens is useful
beyond editing and composition: the cache is a readable, writable record of what a model has already
concluded.
